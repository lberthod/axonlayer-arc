# ---------------------------------------------------------------------------
# Frontend configuration — Agent-to-Agent USDC Task Network
# Copy to `.env` or `.env.local`. Vite only exposes variables prefixed with
# VITE_ to client-side code.
# ---------------------------------------------------------------------------

# Base URL of the backend API. Leave unset to use the default
# http://localhost:3001/api
VITE_API_BASE_URL=http://localhost:3001/api

# --- Firebase Web SDK ------------------------------------------------------
# Grab these from Firebase console → Project settings → Your apps (web).
VITE_FIREBASE_API_KEY=AIzaSyAi6E_b6YtWinW_HzklSwh3kt-GbZRIQMo
VITE_FIREBASE_AUTH_DOMAIN=agenthubarc.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=agenthubarc
VITE_FIREBASE_APP_ID=1:332842304642:web:c511d7c600489655e0209b
